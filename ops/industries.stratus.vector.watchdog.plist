<?xml version="1.0" encoding="UTF-8"?>
<!--
  Watchdog: polls /health/deep and SIGTERMs the backend on repeated
  failure so launchd's KeepAlive picks it back up. Independent from
  the backend service (KeepAlive on the watchdog itself) so that a
  crashed watchdog also auto-recovers.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>industries.stratus.vector.watchdog</string>

    <key>ProgramArguments</key>
    <array>
        <string>__REPO__/ops/watchdog.sh</string>
    </array>

    <key>WorkingDirectory</key>
    <string>__REPO__</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
    </dict>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <key>ThrottleInterval</key>
    <integer>15</integer>

    <key>StandardOutPath</key>
    <string>__HOME__/Library/Logs/Vector/watchdog.out.log</string>

    <key>StandardErrorPath</key>
    <string>__HOME__/Library/Logs/Vector/watchdog.err.log</string>

    <key>ProcessType</key>
    <string>Background</string>

    <key>LowPriorityIO</key>
    <true/>
</dict>
</plist>
